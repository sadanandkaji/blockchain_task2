type ReportCard = record {
  student_name: text;
  total_marks: nat32;
  num_subjects: nat32;
  average: float32;
  grade: text;
};

service : {
  store_report_card: (text, nat32, nat32) -> ();
  get_my_reports: () -> (vec ReportCard);
}
